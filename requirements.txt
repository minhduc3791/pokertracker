# Core dependencies
psutil>=5.9.0
watchdog>=3.0.0
flask>=3.0.0

# Windows-only dependencies (install on Windows for GUI features)
# pip install transparent-overlay pywin32 PyQt5 pyinstaller
# transparent-overlay>=2.8.0
# pywin32>=308
# PyQt5>=5.15.0
# pyinstaller>=6.0.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
